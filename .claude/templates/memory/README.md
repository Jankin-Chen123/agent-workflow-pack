# Local Memory Templates

These files are templates for local user memory.

Copy them into `.claude/memory/` when local memory is needed. The `.claude/memory/` directory is ignored by Git and must not be committed.
